# Arquivo: {% if cookiecutter.use_hydra == 'y' %}config/config.yaml{% endif %}
# Para que serve: Arquivo de configuração central para o Hydra.

# Configuração da API
api:
  host: 0.0.0.0
  port: 8000

# Configuração do Modelo
model:
  name: 'default-model'
  temperature: 0.7